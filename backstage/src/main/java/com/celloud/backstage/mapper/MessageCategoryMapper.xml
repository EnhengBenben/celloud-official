<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.celloud.backstage.mapper.MessageCategoryMapper" >
  <resultMap id="BaseResultMap" type="com.celloud.backstage.model.MessageCategory" >
    <id column="id" property="id" jdbcType="INTEGER" />
    <result column="code" property="code" jdbcType="VARCHAR" />
    <result column="name" property="name" jdbcType="VARCHAR" />
    <result column="email" property="email" jdbcType="INTEGER" />
    <result column="window" property="window" jdbcType="INTEGER" />
    <result column="wechat" property="wechat" jdbcType="INTEGER" />
  </resultMap>
  <sql id="Base_Column_List" >
    id, code, name, email, window, wechat
  </sql>
  <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="java.lang.Integer" >
    select 
    <include refid="Base_Column_List" />
    from tb_message_category
    where id = #{id,jdbcType=INTEGER}
  </select>
  <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer" >
    delete from tb_message_category
    where id = #{id,jdbcType=INTEGER}
  </delete>
  <insert id="insert" parameterType="com.celloud.backstage.model.MessageCategory" >
    insert into tb_message_category (id, code, name, email, 
      window, wechat)
    values (#{id,jdbcType=INTEGER}, #{code,jdbcType=VARCHAR}, #{name,jdbcType=VARCHAR}, #{email,jdbcType=INTEGER}, 
      #{window,jdbcType=INTEGER}, #{wechat,jdbcType=INTEGER})
  </insert>
  <insert id="insertSelective" parameterType="com.celloud.backstage.model.MessageCategory" >
    insert into tb_message_category
    <trim prefix="(" suffix=")" suffixOverrides="," >
      <if test="id != null" >
        id,
      </if>
      <if test="code != null" >
        code,
      </if>
      <if test="name != null" >
        name,
      </if>
      <if test="email != null" >
        email,
      </if>
      <if test="window != null" >
        window,
      </if>
      <if test="wechat != null" >
        wechat,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides="," >
      <if test="id != null" >
        #{id,jdbcType=INTEGER},
      </if>
      <if test="code != null" >
        #{name,jdbcType=VARCHAR},
      </if>
      <if test="name != null" >
        #{name,jdbcType=VARCHAR},
      </if>
      <if test="email != null" >
        #{email,jdbcType=INTEGER},
      </if>
      <if test="window != null" >
        #{window,jdbcType=INTEGER},
      </if>
      <if test="wechat != null" >
        #{wechat,jdbcType=INTEGER},
      </if>
    </trim>
  </insert>
  <update id="updateByPrimaryKeySelective" parameterType="com.celloud.backstage.model.MessageCategory" >
    update tb_message_category
    <set >
      <if test="code != null" >
        code = #{code,jdbcType=VARCHAR},
      </if>
      <if test="name != null" >
        name = #{name,jdbcType=VARCHAR},
      </if>
      <if test="email != null" >
        email = #{email,jdbcType=INTEGER},
      </if>
      <if test="window != null" >
        window = #{window,jdbcType=INTEGER},
      </if>
      <if test="wechat != null" >
        wechat = #{wechat,jdbcType=INTEGER},
      </if>
    </set>
    where id = #{id,jdbcType=INTEGER}
  </update>
  <update id="updateByPrimaryKey" parameterType="com.celloud.backstage.model.MessageCategory" >
    update tb_message_category
    set name = #{name,jdbcType=VARCHAR},
      code = #{code,jdbcType=VARCHAR},
      email = #{email,jdbcType=INTEGER},
      window = #{window,jdbcType=INTEGER},
      wechat = #{wechat,jdbcType=INTEGER}
    where id = #{id,jdbcType=INTEGER}
  </update>
  <select id="findAll" resultType="com.celloud.backstage.model.MessageCategory">
    select 
    <include refid="Base_Column_List"/>
    from tb_message_category
  </select>
  <select id="findUserMessageCategoryByUserId" resultType="java.util.HashMap">
    select umcr.id, umcr.email, umcr.window, umcr.wechat, umcr.user_id,
        umcr.message_category_id mcId, mc.name ,mc.code, 1 flag
    from tb_user_message_category_relat umcr 
    left join tb_message_category mc 
    on mc.id = umcr.message_category_id
    where user_id = #{userId}
  </select>
  <insert id="insertUserMessageCategoryRelat">
    insert into tb_user_message_category_relat(email, window, wechat, message_category_id, user_id) values 
        (${data},#{userId})
  </insert>
  <update id="updateSwitch">
    update tb_user_message_category_relat
    set ${targetSwitch} = #{targetVal}
    where message_category_id = #{relatId} and user_id = #{userId}
  </update>
  <select id="getUserSwitch" resultType="java.lang.Integer">
    select ${targetSwitch} from tb_user_message_category_relat
    where user_id = #{userId} and message_category_id = #{messageCategoryId}
  </select>
  <select id="getDefaultSwitch" resultType="java.lang.Integer">
    select ${targetSwitch} from tb_message_category
    where id = #{messageCategoryId}
  </select>
  <select id="findUserMessageCategoryNotInIds" resultType="java.util.HashMap">
    select mc.id mcId, mc.code, mc.name, mc.email, mc.window, mc.wechat, 0 flag
    from tb_message_category mc 
    where mc.id not in (${ids})
  </select>
  <select id="findUserMessageCategoryBeanByUserId" resultType="com.celloud.backstage.model.MessageCategory">
    select umcr.email, umcr.window, umcr.wechat, umcr.user_id,
        umcr.message_category_id id, mc.name ,mc.code, 1 flag
    from tb_user_message_category_relat umcr 
    left join tb_message_category mc 
    on mc.id = umcr.message_category_id
    where user_id = #{userId}
  </select>
  <select id="findUserMessageCategoryBeanNotInIds" resultType="com.celloud.backstage.model.MessageCategory">
    select mc.id id, mc.code, mc.name, mc.email, mc.window, mc.wechat, 0 flag
    from tb_message_category mc 
    where mc.id not in (${ids})
  </select>
</mapper>