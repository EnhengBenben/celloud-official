#parse("/default/${hcv.appId}/reference.vm")
<div id="printMain">
    #if(${printContext})
        ${printContext}
    #end
    #if(!${printContext})
        <div>
            #parse("/57/${hcv.appId}/title.vm")
            #parse("/default/${hcv.appId}/content.vm")
            #parse("/default/${hcv.appId}/foot.vm")
            </form>
        </div>
    #end
</div>  
#parse("/default/${hcv.appId}/js.vm")